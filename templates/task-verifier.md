<!-- 用法：整份复制到临时目录，逐个 <> 填空，然后删掉本注释块。
     小节顺序和标题不许改、不许删。某节没内容写「无」。
     派 verifier 时不要加 --max-chars——它的返回本来就是压缩过的，
     而那些命令原文正是验收要吃的证据，裁掉等于白跑。 -->

# 验证任务：<一句话，说清验的是哪次改动>

## 你的处境

你看不到派活方的对话，本文件是你拿到的全部上下文。
你只跑门禁、只如实回报，不改任何文件（包括不「顺手」格式化、不 `--fix`）。

## 这次改了什么

| 文件 | 改动性质 |
|---|---|
| <路径> | <例：新增函数 / 改了构建配置 / 只动注释> |

<一句话说改动的性质，让你能判断该加跑什么：动了界面 / 动了构建产物 / 动了依赖 / 纯逻辑。>

## 环境（照抄，不许自己换）

- 解释器 / 运行器：<绝对路径>
- 工作目录：<路径>
- 必须的环境变量：<KEY=VALUE，没有就写「无」>

**环境对不上时不要自己换一个凑合跑**——换了就在返回里明写换了什么，
否则派活方会以为验的是任务书里那套环境。

## 必须跑的门禁

<逐条列。命令原样照抄，不许改参数、不许加 --fix、不许临时绕过规则集。>

```
<命令一>
<命令二>
```

## 按改动范围加跑

<只留适用的，不适用的删掉>

- 动了界面 → 跑项目的截图/视觉回归工具，并且**真的把图看一遍**，不是只看退出码。
- 动了构建产物 / 打包配置 / 依赖 / 路径解析 → 再跑一次真实构建 + 产物冒烟。
  只验 import、只跑开发模式不算通过。
- 一律要跑：`git diff --check` 干净。

## 已知情况

<已登记的偶发红守卫、已知会 skip 的项、跑之前的前置步骤。没有就写「无」。>

## 红线

- **不改任何文件。**跑门禁需要落盘的产物（构建输出、测试缓存）除外，
  但源码一个字都不许动。
- **不跑会抢占真实桌面或改动外部状态的用例**——模拟鼠标键盘、抢窗口焦点、
  发真实网络请求、写生产数据库。需要跑就回报，由派活方决定。
- 不 git commit、不 git add。

## 返回什么

一张表 + 表下三节。

| 检查项 | 结果 | 命令 | 关键输出行 |
|---|---|---|---|
| <名字> | PASS / FAIL / UNAVAILABLE | <照抄你实际跑的整条命令> | <该命令输出里的结论行原文> |

**「关键输出行」一栏不许空、不许写「已执行」「通过了」。**
必须是那条命令真实打出来的字，例如 `440 passed`、`All checks passed!`、`EXIT=0`。
一个字都贴不出来的项，结果就不是 PASS，是 UNAVAILABLE。

表下按顺序补：

1. **每个 FAIL 的完整报错原文** —— 不截断关键堆栈。
2. **每个 UNAVAILABLE 的原因** —— 写清是缺什么。
3. **和本次改动无关、但你撞见的问题** —— 有就写，没有写「无」。
4. **你实际用的环境与任务书不符的地方** —— 有就写，没有写「无」。
