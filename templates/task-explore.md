<!-- 用法：整份复制到临时目录，逐个 <> 填空，然后删掉本注释块。
     小节顺序和标题不许改、不许删。
     只在「这事牵扯到哪些地方我完全没数」时才派 explore；
     已经知道改动就在一两个文件里的活，自己 Glob/Grep 更快。
     派它用 --max-chars 3000。 -->

# 定位任务：<一句话，说清要找什么>

## 你的处境

你看不到派活方的对话，本文件是你拿到的全部上下文。

**你只做定位，不做判断。**返回文件清单，不返回结论、不给修改建议、不评价代码好坏。
派活方会自己去读你列出的文件原文——你的清单是线索，不是证据。

## 要找什么

<写具体。不要写「和 X 相关的代码」，要写「定义了 X 的地方」「调用了 X 的地方」
 「读写 X 这个字段的地方」「注册 X 的地方」这种能对着搜的东西。>

- <目标一>
- <目标二>

## 搜索范围

- 仓库根：<路径>
- 重点看：<目录/glob>
- **不用看**：<例：node_modules、dist、第三方 vendor 目录、生成代码目录>

## 已经知道的（不用再找）

<派活方已经手上有的文件，列出来免得它重复劳动。没有就写「无」。>

## 返回什么

**一份清单，仅此而已。**每行一条：

```
<路径>:<行号>  —— <一句话说为什么相关>
```

规矩：

- 按相关度排序，最相关的在前。
- 行号要准，指到真正相关的那一行。
- 一句话只说**它是什么**（"这里定义了 XxxBridge 的基类"），
  不说**该怎么改**、不说**有什么问题**。
- 找不到就写「没找到」，并列出你搜过的关键词——**不要为了凑数塞不相关的文件**。
- 清单之外不要写总结段、不要写「综上所述」。

## 红线

- 不改任何文件，不 git commit、不 git add。
- 不下结论、不给方案。有想法也咽回去——设计审查不接受外部输入。
